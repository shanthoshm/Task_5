import { TransactionListingResponse } from './transaction-listing-response';

describe('TransactionListingResponse', () => {
  it('should create an instance', () => {
    expect(new TransactionListingResponse()).toBeTruthy();
  });
});
