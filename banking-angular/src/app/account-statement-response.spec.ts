import { AccountStatementResponse } from './account-statement-response';

describe('AccountStatementResponse', () => {
  it('should create an instance', () => {
    expect(new AccountStatementResponse()).toBeTruthy();
  });
});
